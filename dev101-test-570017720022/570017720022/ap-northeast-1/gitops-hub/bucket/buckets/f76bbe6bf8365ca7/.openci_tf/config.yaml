# Written by config0 write-back. state_key mirrors this folder's path
# in the repository (frozen at first write; renames never move state).
# OpenTofu/Terraform >= 1.10 recommended: the platform locks this state with the S3 native lock file (use_lockfile); an older CLI runs UNLOCKED against the same state.
account_alias: dev101-test-570017720022
state_bucket: config0-stateful-62635338
state_key: targets/williaumwu/config0-e2e-gitops/dev101-test-570017720022/570017720022/ap-northeast-1/gitops-hub/bucket/buckets/f76bbe6bf8365ca7/terraform.tfstate
apply:
  allow: true
destroy:
  allow: true
